# Core web framework (Flask is in stdlib-adjacent tier and available)
flask>=3.0.0

# PDF parsing
pdfplumber>=0.11.0

# Environment variables
python-dotenv>=1.0.0

# CLI
click>=8.0.0

# Optional: Streamlit UI (install separately if desired)
# streamlit>=1.35.0

# Optional: If you want to upgrade to FastAPI later
# fastapi>=0.111.0
# uvicorn[standard]>=0.30.1
# pydantic>=2.7.1

# ── LLM API ──
# The system calls Gemini Flash or Anthropic Claude via plain urllib HTTP.
# No extra package required — just set your API key in .env:
#   GEMINI_API_KEY=your_key   (primary, free at https://aistudio.google.com/app/apikey)
#   ANTHROPIC_API_KEY=your_key (fallback)
#
# For offline/demo mode: USE_MOCK_LLM=true (no API key needed)

# ── Vector Store ──
# Uses a lightweight JSON chunk store (no chromadb required).
# To upgrade to ChromaDB when available: pip install chromadb
# then swap app/vector_store.py for the chromadb implementation.
